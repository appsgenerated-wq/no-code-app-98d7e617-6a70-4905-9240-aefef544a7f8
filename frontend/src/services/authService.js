// This file is intentionally left empty as per the critical requirement to use the Manifest SDK directly in AuthContext.
// All authentication logic (login, logout, register, me) is handled in `src/context/AuthContext.jsx`.
const authService = {};
export default authService;
